import subprocess as yeet
main = input("What is your username, bwayne?")
if input('Change all passwords to "Your%Mom%Is%Fucking%Gay69&420? (y/n)"' == ("y" or "Y"):
	 x = yeet.run(["cut -d: -f1 /etc/passwd | wc -l"], shell=True)
	 for i in x:
	 	yeet.run(["cut -d: -f1 /etc/passwd | sed '" + i + "!d'"], shell=False
	 	user = yeet.run(["!!"], shell=True)
		print(user)
